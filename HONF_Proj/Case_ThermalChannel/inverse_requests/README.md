# ThermalChannel inverse request examples

These schema-v1 examples use only the seven supported functional names and
four supported relations. Each has 2–3 active tokens, at most one rectangular
regional token, and a separate geometry block. The reference context matches
the current frozen forward checkpoint domain; target values are illustrative
and exact feasibility is established only by frozen-HONF candidate evaluation.
