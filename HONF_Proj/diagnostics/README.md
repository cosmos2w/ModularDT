# Published and historical diagnostic evidence

This directory contains curated reports and already-published evidence.
Maintained Python source is in `tools/diagnostics`; top-level Python files here
are compatibility wrappers only. New scratch/generated output belongs under
the ignored `diagnostics/generated/` root or the canonical run/evaluation
artifact layout. Existing Stage-1--7 evidence is retained unchanged.
